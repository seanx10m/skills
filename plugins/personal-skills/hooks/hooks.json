{
  "description": "Notice when the skills marketplace has newer commits than what is installed",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/handlers/check-update.sh"
          }
        ]
      }
    ]
  }
}
